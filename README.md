# 🎓 Laura — Освітня платформа

Преміальна освітня платформа **Лаури Александровської** — лендинг + LMS (система управління навчанням) з панеллю студента та адміністратора.

![Next.js](https://img.shields.io/badge/Next.js-16.1.6-black?logo=next.js)
![React](https://img.shields.io/badge/React-19.2.3-61DAFB?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5-3178C6?logo=typescript)
![Tailwind](https://img.shields.io/badge/Tailwind_CSS-4-06B6D4?logo=tailwindcss)

---

## 📋 Зміст

- [Огляд проєкту](#-огляд-проєкту)
- [Технології](#-технології)
- [Структура проєкту](#-структура-проєкту)
- [Швидкий старт](#-швидкий-старт)
- [Маршрути та сторінки](#-маршрути-та-сторінки)
- [Архітектура](#-архітектура)
- [Теми оформлення](#-теми-оформлення)
- [Скрипти](#-скрипти)
- [Деплой](#-деплой)

---

## 🎯 Огляд проєкту

Платформа складається з трьох основних частин:

| Розділ | URL | Опис |
|--------|-----|------|
| **Лендинг** | `/` | Промо-сторінка з hero-секцією, відео, курсами, відгуками, CTA |
| **Кабінет студента** | `/dashboard` | Мої курси, прогрес, уроки, домашні завдання, налаштування |
| **Панель адміністратора** | `/admin` | Управління студентами, курсами, перевірка ДЗ, статистика |

---

## 🛠 Технології

| Категорія | Технологія |
|-----------|-----------|
| Фреймворк | **Next.js 16** (App Router, Turbopack) |
| UI | **React 19** + **TypeScript 5** |
| Стилі | **Tailwind CSS 4** + CSS Custom Properties |
| Анімації | **Framer Motion 12** — scroll-анімації, `AnimatePresence`, `layoutId` |
| 3D | **Three.js** + **React Three Fiber** + **Drei** — 3D hero-фон |
| Стейт-менеджмент | **Zustand 5** — auth-store, lms-store |
| Скролінг | **Lenis** — плавний скрол |
| Іконки | **Lucide React** |
| Безпека | **DOMPurify** — захист від XSS у HTML-контенті уроків |
| Маркі | **react-fast-marquee** — біжучий рядок |
| Шрифти | Inter Tight, Playfair Display, Montserrat (кириличні підмножини) |

---

## 📁 Структура проєкту

```
src/
├── app/
│   ├── page.tsx                          # Лендинг (головна сторінка)
│   ├── layout.tsx                        # Кореневий layout + шрифти + ThemeProvider
│   ├── globals.css                       # Глобальні стилі, CSS-змінні, теми
│   │
│   ├── dashboard/                        # 🎒 Кабінет студента
│   │   ├── layout.tsx                    #    Sidebar + мобільна навігація
│   │   ├── page.tsx                      #    Головна — привітання, статистика, мої курси
│   │   ├── courses/
│   │   │   ├── page.tsx                  #    Список всіх курсів з прогресом
│   │   │   └── [slug]/
│   │   │       ├── page.tsx              #    Деталі курсу — модулі, акордеон уроків
│   │   │       └── lesson/
│   │   │           └── [lessonId]/
│   │   │               └── page.tsx      #    Плеєр уроку — відео, зміст, ДЗ
│   │   ├── homework/
│   │   │   └── page.tsx                  #    Мої домашні завдання та оцінки
│   │   └── settings/
│   │       └── page.tsx                  #    Налаштування профілю
│   │
│   └── admin/                            # 🔧 Панель адміністратора
│       ├── layout.tsx                    #    Sidebar адміна
│       ├── page.tsx                      #    Дашборд — статистика платформи
│       ├── students/
│       │   └── page.tsx                  #    Список студентів
│       ├── courses/
│       │   └── page.tsx                  #    Управління курсами
│       └── homework/
│           └── page.tsx                  #    Перевірка домашніх завдань
│
├── components/                           # 🧩 Компоненти лендингу
│   ├── Hero.tsx                          #    Hero-секція з TypewriterText
│   ├── HeroBackground3D.tsx             #    3D анімований фон (Three.js)
│   ├── TypewriterText.tsx               #    Ефект друкарської машинки
│   ├── About.tsx                         #    Секція «Про мене»
│   ├── Stats.tsx                         #    Статистика в цифрах
│   ├── Courses.tsx                       #    Каталог курсів
│   ├── CourseGrid.tsx                    #    Сітка карток курсів
│   ├── Products.tsx                      #    Продукти / послуги
│   ├── Philosophy.tsx                    #    Філософія навчання
│   ├── Testimonials.tsx                  #    Відгуки студентів
│   ├── CTA.tsx                           #    Call-to-Action секція
│   ├── Header.tsx                        #    Навігація лендингу
│   ├── Footer.tsx                        #    Футер
│   ├── InfiniteMarquee.tsx              #    Нескінченний маркі-рядок
│   ├── MarqueeBand.tsx                  #    Стрічка з текстом
│   ├── VideoBackground.tsx              #    Фонове відео
│   ├── ThemeProvider.tsx                #    Контекст теми (dark/light)
│   ├── ThemeSwitcher.tsx                #    Перемикач теми
│   ├── AccentSwitcher.tsx               #    Перемикач акцентного кольору
│   └── SmoothScroll.tsx                 #    Обгортка Lenis для плавного скролу
│
├── stores/                               # 📦 Zustand сховища
│   ├── auth-store.ts                    #    Авторизація — user, role, login/logout
│   ├── lms-store.ts                     #    LMS — курси, уроки, прогрес, ДЗ, адмін-дії
│   └── index.ts                         #    Реекспорт
│
├── lib/
│   └── mock-data.ts                     #    Мок-дані — курси, модулі, уроки, студенти
│
└── types/
    └── lms.ts                           #    TypeScript типи — 18 інтерфейсів, 6 енамів
```

---

## 🚀 Швидкий старт

### Вимоги

- **Node.js** ≥ 18
- **npm** ≥ 9

### Встановлення

```bash
# Клонувати репозиторій
git clone <url-репозиторію>
cd laura-marketing-courses

# Встановити залежності
npm install
```

### Запуск (режим розробки)

```bash
npm run dev
```

Відкрийте [http://localhost:3000](http://localhost:3000) у браузері.

> 💡 За замовчуванням використовується **Turbopack** для швидкої збірки.

### Збірка для продакшену

```bash
npm run build
npm start
```

---

## 🗺 Маршрути та сторінки

### Лендинг (`/`)

Промо-сторінка з секціями:
1. **Hero** — анімований заголовок (ефект друкарської машинки) + 3D фон
2. **Маркі-стрічка** — біжучий текст із ключовими словами
3. **Про мене** — інформація про Лауру
4. **Статистика** — цифри досягнень
5. **Курси** — каталог доступних курсів
6. **Продукти** — послуги та матеріали
7. **Філософія** — підхід до навчання
8. **Відгуки** — слайдер з відгуками студентів
9. **CTA** — заклик до дії
10. **Футер** — контакти, соцмережі

### Кабінет студента (`/dashboard`)

| Сторінка | Маршрут | Функціонал |
|----------|---------|------------|
| Головна | `/dashboard` | Привітання, швидка статистика (курси / уроки / прогрес), картки активних курсів із прогрес-рингом |
| Мої курси | `/dashboard/courses` | Повний список курсів із прогрес-барами та статусами |
| Деталі курсу | `/dashboard/courses/[slug]` | Модулі-акордеони, список уроків, іконки статусу (✅ пройдено / 🔒 заблоковано) |
| Плеєр уроку | `/dashboard/courses/[slug]/lesson/[lessonId]` | Відео-плеєр, 3 вкладки (зміст / ресурси / домашнє завдання), кнопки «Попередній / Наступний урок», позначка «Завершено» |
| Домашні завдання | `/dashboard/homework` | Список відправлених ДЗ, статуси (очікує / перевірено / потребує доопрацювання), оцінки |
| Налаштування | `/dashboard/settings` | Редагування профілю (placeholder) |

### Панель адміністратора (`/admin`)

| Сторінка | Маршрут | Функціонал |
|----------|---------|------------|
| Дашборд | `/admin` | Загальна статистика платформи — кількість студентів, курсів, ДЗ на перевірку |
| Студенти | `/admin/students` | Список усіх студентів із фільтрацією та пошуком |
| Курси | `/admin/courses` | Управління курсами — перегляд, статуси, кількість модулів/уроків |
| Домашні завдання | `/admin/homework` | Перевірка ДЗ студентів — виставлення оцінок, коментарі |

---

## 🏗 Архітектура

### Стейт-менеджмент (Zustand)

```
┌─────────────────┐     ┌──────────────────┐
│   auth-store    │     │    lms-store     │
├─────────────────┤     ├──────────────────┤
│ • user          │     │ • courses        │
│ • role          │     │ • enrollments    │
│ • isAuth        │     │ • progress       │
│ • login()       │     │ • submissions    │
│ • logout()      │     │ • completeLesson │
│ • initialize()  │     │ • submitHomework │
└─────────────────┘     │ • gradeHomework  │
                        │ • initialize()   │
                        └──────────────────┘
```

- **auth-store** — керування авторизацією. При переході на `/dashboard` автоматично логінить як студента, на `/admin` — як адміністратора (мок-режим).
- **lms-store** — усі дані LMS: курси, модулі, уроки, записи на курси, прогрес проходження, домашні завдання та їх перевірка.

### Типи даних (`src/types/lms.ts`)

**18 інтерфейсів:**
`User`, `Course`, `Module`, `Lesson`, `Enrollment`, `LessonProgress`, `HomeworkSubmission`, `HomeworkReview`, `Resource`, `CourseWithModules`, `ModuleWithLessons`, `LessonWithResources`, `EnrollmentWithCourse`, `ProgressWithLesson`, `SubmissionWithDetails`, `AdminStats`, `StudentWithEnrollments`, `LMSState`

**6 енамів:**
`UserRole`, `CourseStatus`, `LessonType`, `HomeworkStatus`, `EnrollmentStatus`, `SubmissionStatus`

### Мок-дані (`src/lib/mock-data.ts`)

Проєкт працює на мок-даних без бекенду. Підготовлено:
- 2 курси з модулями та уроками
- Студентські записи та прогрес
- Домашні завдання з відгуками
- SQL-схема для Supabase (у `supabase/schema.sql`)

---

## 🎨 Теми оформлення

Платформа підтримує **дві теми** з миттєвим перемиканням без FOUC (мерехтіння):

| Властивість | Dark (за замовчуванням) | Light |
|-------------|------------------------|-------|
| `--bg` | `#0A0A0A` | `#FFFFFF` |
| `--fg` | `#FFFFFF` | `#0A0A0A` |
| `--muted` | `#999999` | `#666666` |
| `--border-color` | `#1A1A1A` | `#E5E5E5` |
| `--secondary` | `#F5F5F5` | `#1A1A1A` |

Тема зберігається в `localStorage` і застосовується **блокуючим скриптом** у `<head>` для запобігання мерехтінню при завантаженні.

Перемикач теми: іконка 🌙/☀️ у хедері лендингу та sidebar дашборду.

---

## 📜 Скрипти

| Команда | Опис |
|---------|------|
| `npm run dev` | Запуск dev-сервера (Turbopack, порт 3000) |
| `npm run build` | Продакшен-збірка |
| `npm start` | Запуск продакшен-сервера |
| `npm run lint` | Перевірка ESLint |

---

## 🔒 Безпека

- **DOMPurify** — весь HTML-контент уроків (вкладка «Зміст») санітизується з білим списком дозволених тегів (`h1`–`h6`, `p`, `ul`, `ol`, `li`, `strong`, `em`, `a`, `br`, `span`, `blockquote`)
- **XSS-захист** — `dangerouslySetInnerHTML` ніде не використовується без попередньої санітизації

---

## ♿ Доступність (A11y)

- Усі інтерактивні елементи мають `aria-label`
- Підтримка навігації клавіатурою (`focus-visible` стилі)
- Семантичні ролі: `role="dialog"`, `aria-modal`, `aria-expanded`, `aria-current="page"`
- Скелетон-завантажувачі з `role="status"` та `sr-only` текстом

---

## 🚀 Деплой

### Vercel (рекомендовано)

```bash
npm install -g vercel
vercel
```

Або підключіть GitHub-репозиторій до [Vercel Dashboard](https://vercel.com/new).

### Docker

```dockerfile
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:18-alpine AS runner
WORKDIR /app
COPY --from=builder /app/.next ./.next
COPY --from=builder /app/public ./public
COPY --from=builder /app/package*.json ./
RUN npm ci --production
EXPOSE 3000
CMD ["npm", "start"]
```

---

## 📝 Ліцензія

Приватний проєкт. Всі права захищені © 2026 Laura Aleksandrovska.
